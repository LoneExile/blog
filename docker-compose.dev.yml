version: '3.9'

services:
  voidbox:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: voidbox
    ports:
      - '3000:3000'
    networks:
      - voidbox_network

networks:
  voidbox_network:
    external: true
