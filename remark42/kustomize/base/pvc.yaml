apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: remark42-pvc
  namespace: default
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi

